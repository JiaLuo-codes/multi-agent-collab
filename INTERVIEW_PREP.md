# 面试话术准备 —— Multi-Agent 协同系统

---

## 1. "一句话介绍这个项目"

我基于 LangGraph 构建了一个 Multi-Agent 协同研究系统，三个 Agent——规划者、搜索者、写作者——通过状态图编排协作完成复杂信息调研任务。核心亮点是规划者会在每轮搜索后评估信息完整性，不够就自动补搜，最多两轮，避免单次搜索信息遗漏。

---

## 2. "为什么用 LangGraph 而不是手动串行调用？"

LangGraph 的 StateGraph 提供了三个关键能力：

- **显式状态管理**：搜索关键词、结果、轮次计数都在 TypedDict 里统一维护，不靠全局变量
- **条件路由**：规划者评估信息充足→直接写作，不足→重新搜索，这个分支逻辑用条件边表达很清晰
- **循环控制**：搜索→评估→补搜 这个环路在 StateGraph 里天然支持，手写的话要自己管理递归和终止条件

如果用串行调用，其实就是把三个 LLM 的 input/output 拼在一起，跟写一个超长 prompt 没有本质区别。

---

## 3. "Multi-Agent 和单 Agent 调多个工具有什么区别？"

单 Agent 调工具：一个模型自己决定调哪个工具、什么时候调。适合简单任务（计算、查时间）。

Multi-Agent：每个 Agent 有独立的角色定义、系统提示词和决策逻辑。规划者负责"想清楚要搜什么"，搜索者只管"搜得准"，写作者专注"写得好"。分工之后：
- 每个 Agent 的 prompt 更聚焦，不容易指令冲突
- 规划者评估信息完整性这个环节是单 Agent 做不到的——单 Agent 调完工具就直接输出了，不会反思"搜到的信息够不够"

---

## 4. "二轮搜索的必要性是什么？怎么避免死循环？"

**为什么需要二轮**：真实搜索场景中，第一轮关键词可能不够精准。比如搜"AI Agent 进展"，结果太泛；规划者看到后调整为"Gartner autonomous agents 2026", "IDC agent adoption forecast" 等更具体的词，第二轮命中率更高。

**怎么避免死循环**：最多 2 轮。2 轮后强制进写作节点，保证系统一定会终止。这个设计是为平衡信息充分性和响应速度。

---

## 5. "有什么可以改进的地方？"

- **引入验证 Agent**：对写作 Agent 的输出做事实核查，减少幻觉
- **动态轮次控制**：根据信息增益决定是否继续搜索，而非固定 2 轮
- **支持更多工具**：RAG 文档检索、数据库查询、代码执行，扩展 Agent 能力边界
- **记忆持久化**：用 LangGraph 的 checkpointer 把对话状态存到数据库，支持长会话

---

## 6. "你从这个项目中学到了什么？"

- 理解了 Agent 的核心不是"调 LLM API"，而是**如何编排多个决策单元完成复杂任务**
- LangGraph 的 StateGraph 模式比硬编码的 if-else 更适合 Agent 系统——状态驱动、可扩展、可调试
- Tool Calling 本身不复杂，难的是工具失败、信息不足、死循环等异常情况的处理——这才是工程化的关键

---

## 7. "你怎么理解智能体？"

智能体是一个能**自主感知环境、做出决策、执行动作、从反馈中学习**的系统。

这个项目里：感知 = 联网搜索获取信息；决策 = 规划者评估信息充足性并制定策略；执行 = 搜索者检索 + 写作者生成报告；反馈 = 规划者看到搜索结果后调整搜索计划。

对比 JD："智能体(Agent)的整体架构设计，包括感知模块、决策规划模块、执行模块"——我的项目就是这三个模块的工程实践。
